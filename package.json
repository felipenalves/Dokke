{
  "name": "dokke",
  "version": "0.2.6",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "mac:install": "cd mac && ./install.sh",
    "mac:open": "cd mac && ./install.sh --open"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "ds-store": "^0.1.6",
    "playwright": "^1.62.1"
  }
}
