{
  "name": "dokke",
  "version": "0.2.8",
  "private": true,
  "type": "module",
  "portless": { "name": "dokke", "script": "start" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "mac:install": "cd mac && ./install.sh",
    "mac:open": "cd mac && ./install.sh --open"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "ds-store": "^0.1.6",
    "playwright": "^1.62.1"
  }
}
